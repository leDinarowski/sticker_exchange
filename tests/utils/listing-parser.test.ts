import { describe, it, expect } from 'vitest';
// import { parseListingInput, formatListingPreview } from '../../src/utils/listing-parser';

// Placeholder — implementation comes in Phase 2 (feat/listing-registration)
describe('listing-parser', () => {
  it('placeholder — remove when implementing the parser', () => {
    expect(true).toBe(true);
  });
});
